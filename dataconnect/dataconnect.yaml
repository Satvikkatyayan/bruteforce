specVersion: "v1"
serviceId: "bruteforce"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bruteforce-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
